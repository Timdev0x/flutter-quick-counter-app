# Quick Flutter App

A minimal Flutter counter app scaffold.

## Run

Open a terminal and run:

```powershell
cd "/your-folder-location/"
flutter pub get
flutter run
```

You can run on a specific device with `flutter run -d <device-id>`.
